{
  "id": "20260910-rule-relay-test-fix-v1",
  "agent_binding": "e75c77cb-7589-4452-94b2-decc97ff85a1",
  "mode": "commands",
  "work_branch": "work/fix-rule-relay-test-20260910",
  "allow_write": true,
  "resources": [],
  "command_timeout": 1200,
  "idle_timeout": 300,
  "task_timeout": 2400,
  "memory_limit_mb": 4096,
  "commands": [
    "set -e; git fetch --prune origin; git checkout -B work/fix-rule-relay-test-20260910 origin/main; test \"$(git rev-parse HEAD)\" = \"88b192366f3b52cc4fe7df5a205343b4fd8d9d0e\"; test -z \"$(git status --porcelain)\"",
    "python3 - <<'PY'\nfrom pathlib import Path\np=Path('packages/shelly-client/src/scripts/install.ts')\ns=p.read_text()\ns=s.replace(\"const BLE_SCANNER_CLEANUP_DELAY_MS = 1000;\", \"const BLE_SCANNER_CLEANUP_DELAY_MS = 1000;\\nconst DEFAULT_RELAY_TEST_ON_DURATION_MS = 1000;\")\nold='''  async safeRelayTest(options?: {\n    onDurationMs?: number;\n  }): Promise<Result<RelayTestResult>> {\n    const onDurationMs = options?.onDurationMs ?? 100;\n    let onCommandSent = false;\n    let offCommandSent = false;\n    let onError: ShellyClientError | undefined;\n    let offError: ShellyClientError | undefined;\n\n    try {\n      const on = await this.setRelayOn();\n      if (!on.ok) {\n        onError = on.error;\n      } else {\n        onCommandSent = true;\n        await new Promise((resolve) => setTimeout(resolve, onDurationMs));\n      }\n    } finally {\n      const off = await this.setRelayOff();\n      offCommandSent = off.ok;\n      if (!off.ok) {\n        offError = off.error;\n      }\n    }\n\n    if (offError) {\n      return {\n        ok: false,\n        error: relayTestError(\n          `Final relay OFF command failed; final state could not be confirmed. ${\n            offError.technicalMessage ?? offError.kind\n          }`\n        )\n      };\n    }\n\n    if (onError) {\n      return { ok: false, error: onError };\n    }\n\n    const status = await this.transport.call<unknown>({\n      method: RPC_METHODS.SwitchGetStatus,\n      params: { id: 0 }\n    });\n    if (!status.ok) {\n      return status;\n    }\n    const parsed = switchStatusSchema.safeParse(status.value);\n    if (!parsed.success) {\n      return { ok: false, error: validationError(parsed.error.message) };\n    }\n    if (parsed.data.output) {\n      return {\n        ok: false,\n        error: relayTestError('Final relay state is ON after the safe relay test.')\n      };\n    }\n\n    return {\n      ok: true,\n      value: {\n        finalRelayOn: parsed.data.output,\n        onCommandSent,\n        offCommandSent\n      }\n    };\n  }\n'''
new='''  async safeRelayTest(options?: {\n    onDurationMs?: number;\n  }): Promise<Result<RelayTestResult>> {\n    const onDurationMs = options?.onDurationMs ?? DEFAULT_RELAY_TEST_ON_DURATION_MS;\n    let onCommandSent = false;\n    let offCommandSent = false;\n    let onError: ShellyClientError | undefined;\n    let onVerificationError: ShellyClientError | undefined;\n    let offError: ShellyClientError | undefined;\n\n    try {\n      const on = await this.setRelayOn();\n      if (!on.ok) {\n        onError = on.error;\n      } else {\n        onCommandSent = true;\n        const onStatus = await this.transport.call<unknown>({\n          method: RPC_METHODS.SwitchGetStatus,\n          params: { id: 0 }\n        });\n        if (!onStatus.ok) {\n          onVerificationError = relayTestError(\n            `Relay ON state could not be confirmed. ${\n              onStatus.error.technicalMessage ?? onStatus.error.kind\n            }`\n          );\n        } else {\n          const parsedOnStatus = switchStatusSchema.safeParse(onStatus.value);\n          if (!parsedOnStatus.success) {\n            onVerificationError = relayTestError(\n              `Relay ON status was invalid. ${parsedOnStatus.error.message}`\n            );\n          } else if (!parsedOnStatus.data.output) {\n            onVerificationError = relayTestError(\n              'Relay did not reach ON state during the safe relay test.'\n            );\n          } else {\n            await this.sleepMs(onDurationMs);\n          }\n        }\n      }\n    } finally {\n      const off = await this.setRelayOff();\n      offCommandSent = off.ok;\n      if (!off.ok) {\n        offError = off.error;\n      }\n    }\n\n    if (offError) {\n      return {\n        ok: false,\n        error: relayTestError(\n          `Final relay OFF command failed; final state could not be confirmed. ${\n            offError.technicalMessage ?? offError.kind\n          }`\n        )\n      };\n    }\n\n    if (onError) {\n      return { ok: false, error: onError };\n    }\n    if (onVerificationError) {\n      return { ok: false, error: onVerificationError };\n    }\n\n    const status = await this.transport.call<unknown>({\n      method: RPC_METHODS.SwitchGetStatus,\n      params: { id: 0 }\n    });\n    if (!status.ok) {\n      return status;\n    }\n    const parsed = switchStatusSchema.safeParse(status.value);\n    if (!parsed.success) {\n      return { ok: false, error: validationError(parsed.error.message) };\n    }\n    if (parsed.data.output) {\n      return {\n        ok: false,\n        error: relayTestError('Final relay state is ON after the safe relay test.')\n      };\n    }\n\n    return {\n      ok: true,\n      value: {\n        finalRelayOn: parsed.data.output,\n        onCommandSent,\n        offCommandSent\n      }\n    };\n  }\n'''
if old not in s: raise SystemExit('safeRelayTest block not found')\np.write_text(s.replace(old,new))\n\np=Path('packages/shelly-client/src/__tests__/shelly-client.test.ts')\ns=p.read_text()\ns=s.replace('''  failOffCommand?: boolean;\n  scriptRunning?: boolean;\n''','''  failOffCommand?: boolean;\n  forceRelayOffStatus?: boolean;\n  scriptRunning?: boolean;\n''')\ns=s.replace('''    if (request.method === RPC_METHODS.SwitchGetStatus) {\n      return { ok: true, value: { id: 0, output: this.relayOn } as TResponse };\n    }\n''','''    if (request.method === RPC_METHODS.SwitchGetStatus) {\n      return {\n        ok: true,\n        value: {\n          id: 0,\n          output: this.options.forceRelayOffStatus ? false : this.relayOn\n        } as TResponse\n      };\n    }\n''')\ns=s.replace('''  it('safe relay test ends OFF', async () => {\n    const transport = new RecordingTransport();\n    const client = new RpcShellyClient(transport);\n    const result = await client.safeRelayTest({ onDurationMs: 0 });\n\n    expect(result.ok).toBe(true);\n    if (!result.ok) {\n      return;\n    }\n    expect(result.value.finalRelayOn).toBe(false);\n    expect(transport.relayOn).toBe(false);\n  });\n''','''  it('safe relay test confirms ON before ending OFF', async () => {\n    const transport = new RecordingTransport();\n    const client = new RpcShellyClient(transport);\n    const result = await client.safeRelayTest({ onDurationMs: 0 });\n\n    expect(result.ok).toBe(true);\n    if (!result.ok) {\n      return;\n    }\n    expect(result.value.finalRelayOn).toBe(false);\n    expect(transport.relayOn).toBe(false);\n    expect(transport.requests.map((request) => request.method)).toEqual([\n      RPC_METHODS.SwitchSet,\n      RPC_METHODS.SwitchGetStatus,\n      RPC_METHODS.SwitchSet,\n      RPC_METHODS.SwitchGetStatus\n    ]);\n  });\n\n  it('fails if the relay never reaches ON and still ends OFF', async () => {\n    const transport = new RecordingTransport({ forceRelayOffStatus: true });\n    const client = new RpcShellyClient(transport);\n    const result = await client.safeRelayTest({ onDurationMs: 0 });\n\n    expect(result.ok).toBe(false);\n    if (result.ok) {\n      return;\n    }\n    expect(result.error.kind).toBe('relay-test-failed');\n    expect(result.error.technicalMessage).toContain('did not reach ON state');\n    expect(transport.relayOn).toBe(false);\n  });\n''')\np.write_text(s)\nPY",
    "pnpm --filter @lcl/shelly-client test",
    "pnpm --filter @lcl/shelly-client typecheck",
    "pnpm --filter @lcl/mobile test -- --run apps/mobile/src/__tests__/hardware-setup.test.tsx",
    "git diff --check; git status --short; git diff -- packages/shelly-client/src/scripts/install.ts packages/shelly-client/src/__tests__/shelly-client.test.ts",
    "git add packages/shelly-client/src/scripts/install.ts packages/shelly-client/src/__tests__/shelly-client.test.ts && git commit -m 'fix(shelly): verify relay test pulse'",
    "pnpm check",
    "set -e; git push -u origin work/fix-rule-relay-test-20260910; test -z \"$(git status --porcelain)\"; echo HEAD=$(git rev-parse HEAD); test \"$(git rev-parse HEAD)\" = \"$(git rev-parse origin/work/fix-rule-relay-test-20260910)\""
  ]
}

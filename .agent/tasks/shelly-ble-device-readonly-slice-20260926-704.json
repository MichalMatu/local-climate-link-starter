{
  "id": "shelly-ble-device-readonly-slice-20260926-704",
  "agent_binding": "e75c77cb-7589-4452-94b2-decc97ff85a1",
  "mode": "commands",
  "work_branch": "work/shelly-ble-transport",
  "allow_write": true,
  "resources": [],
  "command_timeout": 1800,
  "idle_timeout": 600,
  "task_timeout": 5400,
  "memory_limit_mb": 4096,
  "commands": [
    "set -euo pipefail; test \"$(git rev-parse HEAD)\" = \"7a7e72b017d5d39e21ef83df8abb09c53a5d143f\"; test -z \"$(git status --short)\"",
    "python3 - <<'PY'\nfrom pathlib import Path\n\np = Path('packages/shelly-client/src/plugsUi.ts')\ns = p.read_text()\nold = '''  async read(): Promise<Result<ShellyPlugsUiReadResult>> {\n    const methodsResponse = await this.transport.call<unknown>({\n      method: RPC_METHODS.ShellyListMethods\n    });\n    if (!methodsResponse.ok) {\n      return methodsResponse;\n    }\n\n    const parsedMethods = listMethodsResponseSchema.safeParse(methodsResponse.value);\n    if (!parsedMethods.success) {\n      return { ok: false, error: validationError(parsedMethods.error.message) };\n    }\n\n    const canRead = parsedMethods.data.methods.includes(RPC_METHODS.PlugsUiGetConfig);\n    const canWrite = parsedMethods.data.methods.includes(RPC_METHODS.PlugsUiSetConfig);\n    if (!canRead || !canWrite) {\n      return { ok: true, value: { supported: false } };\n    }\n\n    const configResponse = await this.transport.call<unknown>({\n      method: RPC_METHODS.PlugsUiGetConfig\n    });\n    if (!configResponse.ok) {\n      return configResponse;\n    }\n\n    const parsedConfig = plugsUiConfigSchema.safeParse(configResponse.value);\n    return parsedConfig.success\n      ? {\n          ok: true,\n          value: {\n            supported: true,\n            config: parsedConfig.data,\n            capabilities: ledCapabilities(parsedConfig.data),\n            controlCapabilities: controlCapabilities(parsedConfig.data)\n          }\n        }\n      : { ok: false, error: validationError(parsedConfig.error.message) };\n  }\n'''\nnew = '''  private async readWithRequirements(\n    requireWrite: boolean\n  ): Promise<Result<ShellyPlugsUiReadResult>> {\n    const methodsResponse = await this.transport.call<unknown>({\n      method: RPC_METHODS.ShellyListMethods\n    });\n    if (!methodsResponse.ok) {\n      return methodsResponse;\n    }\n\n    const parsedMethods = listMethodsResponseSchema.safeParse(methodsResponse.value);\n    if (!parsedMethods.success) {\n      return { ok: false, error: validationError(parsedMethods.error.message) };\n    }\n\n    const canRead = parsedMethods.data.methods.includes(RPC_METHODS.PlugsUiGetConfig);\n    const canWrite = parsedMethods.data.methods.includes(RPC_METHODS.PlugsUiSetConfig);\n    if (!canRead || (requireWrite && !canWrite)) {\n      return { ok: true, value: { supported: false } };\n    }\n\n    const configResponse = await this.transport.call<unknown>({\n      method: RPC_METHODS.PlugsUiGetConfig\n    });\n    if (!configResponse.ok) {\n      return configResponse;\n    }\n\n    const parsedConfig = plugsUiConfigSchema.safeParse(configResponse.value);\n    return parsedConfig.success\n      ? {\n          ok: true,\n          value: {\n            supported: true,\n            config: parsedConfig.data,\n            capabilities: ledCapabilities(parsedConfig.data),\n            controlCapabilities: controlCapabilities(parsedConfig.data)\n          }\n        }\n      : { ok: false, error: validationError(parsedConfig.error.message) };\n  }\n\n  async readConfig(): Promise<Result<ShellyPlugsUiReadResult>> {\n    return this.readWithRequirements(false);\n  }\n\n  async read(): Promise<Result<ShellyPlugsUiReadResult>> {\n    return this.readWithRequirements(true);\n  }\n'''\nif old not in s:\n    raise SystemExit('plugsUi read anchor not found')\np.write_text(s.replace(old, new, 1))\n\np = Path('apps/mobile/src/features/plugs/data/plugReadOnlyManagementTarget.ts')\ns = p.read_text()\nold = '''export const withVerifiedPlugReadOnlyClient = async <T>(\n  target: PlugReadOnlyManagementTarget,\n  work: (client: ReadOnlyShellyClient) => Promise<T>,\n  dependencies: PlugReadOnlyManagementDependencies = defaultDependencies\n): Promise<T> => {\n  if (target.transport === 'wifi') {\n    const transport = await dependencies.createWifiTransport({\n      deviceId: target.physicalId,\n      baseUrl: target.baseUrl\n    });\n    return work(dependencies.createClient(transport));\n  }\n\n  const transport = dependencies.createBleTransport(target.bleDeviceId);\n  try {\n    const client = dependencies.createClient(transport);\n    await assertMatchingPhysicalIdentity(target, client);\n    return await work(client);\n  } finally {\n    await transport.disconnect().catch(() => undefined);\n  }\n};\n'''\nnew = '''export const withVerifiedPlugReadOnlyTransport = async <T>(\n  target: PlugReadOnlyManagementTarget,\n  work: (transport: ShellyRpcTransport) => Promise<T>,\n  dependencies: PlugReadOnlyManagementDependencies = defaultDependencies\n): Promise<T> => {\n  if (target.transport === 'wifi') {\n    const transport = await dependencies.createWifiTransport({\n      deviceId: target.physicalId,\n      baseUrl: target.baseUrl\n    });\n    return work(transport);\n  }\n\n  const transport = dependencies.createBleTransport(target.bleDeviceId);\n  try {\n    const client = dependencies.createClient(transport);\n    await assertMatchingPhysicalIdentity(target, client);\n    return await work(transport);\n  } finally {\n    await transport.disconnect().catch(() => undefined);\n  }\n};\n\nexport const withVerifiedPlugReadOnlyClient = async <T>(\n  target: PlugReadOnlyManagementTarget,\n  work: (client: ReadOnlyShellyClient) => Promise<T>,\n  dependencies: PlugReadOnlyManagementDependencies = defaultDependencies\n): Promise<T> =>\n  withVerifiedPlugReadOnlyTransport(\n    target,\n    async (transport) => work(dependencies.createClient(transport)),\n    dependencies\n  );\n'''\nif old not in s:\n    raise SystemExit('read-only target anchor not found')\np.write_text(s.replace(old, new, 1))\n\np = Path('apps/mobile/src/features/plugs/screens/BlePlugDetailScreen.tsx')\ns = p.read_text()\ns = s.replace("import { PlugInfoPanel } from '../components/PlugInfoPanel.js';", "import { BlePlugDeviceReadOnlyPanel } from '../components/BlePlugDeviceReadOnlyPanel.js';\\nimport { PlugInfoPanel } from '../components/PlugInfoPanel.js';", 1)\ns = s.replace("import { useBlePlugInformationFlow } from '../flows/useBlePlugInformationFlow.js';", "import { useBlePlugDeviceSettingsFlow } from '../flows/useBlePlugDeviceSettingsFlow.js';\\nimport { useBlePlugInformationFlow } from '../flows/useBlePlugInformationFlow.js';", 1)\ns = s.replace('  const informationQuery = useBlePlugInformationFlow(plug);', '  const informationQuery = useBlePlugInformationFlow(plug);\\n  const deviceSettingsQuery = useBlePlugDeviceSettingsFlow(plug);', 1)\nold = '''      <section className=\"plug-detail-surface\" aria-label={t('common.info')}>\n        <PlugInfoPanel\n'''\nnew = '''      <section className=\"plug-detail-surface\">\n        <BlePlugDeviceReadOnlyPanel\n          settings={deviceSettingsQuery.data}\n          loading={deviceSettingsQuery.isPending}\n          error={deviceSettingsQuery.isError}\n        />\n        <PlugInfoPanel\n'''\nif old not in s:\n    raise SystemExit('BLE detail render anchor not found')\np.write_text(s.replace(old, new, 1))\n\np = Path('packages/shelly-client/src/__tests__/plugsUi.test.ts')\ns = p.read_text()\nanchor = '''  it('returns a clean unsupported state without calling PLUGS_UI', async () => {\n'''\ninsert = '''  it('reads config without requiring mutation support', async () => {\n    const transport = new RecordingTransport([\n      { ok: true, value: { methods: ['PLUGS_UI.GetConfig'] } },\n      { ok: true, value: fullConfig }\n    ]);\n    const client = new RpcShellyPlugsUiClient(transport);\n\n    await expect(client.readConfig()).resolves.toMatchObject({\n      ok: true,\n      value: { supported: true, config: fullConfig }\n    });\n    expect(transport.requests).toEqual([\n      { method: 'Shelly.ListMethods' },\n      { method: 'PLUGS_UI.GetConfig' }\n    ]);\n  });\n\n''' + anchor\nif anchor not in s:\n    raise SystemExit('plugsUi test anchor not found')\np.write_text(s.replace(anchor, insert, 1))\nPY",
    "python3 - <<'PY'\nfrom pathlib import Path\nfiles = {\n'apps/mobile/src/features/plugs/data/plugDeviceReadOnlySettings.ts': '''import {\n  RpcShellyPlugsUiClient,\n  type ShellyPlugsUiReadResult\n} from '@lcl/shelly-client';\nimport { unwrapShellyResult } from '../../../platform/shellyResult.js';\nimport { unwrapBlePlugReadOnlyResult } from './blePlugReadOnlyError.js';\nimport {\n  withVerifiedPlugReadOnlyTransport,\n  type PlugReadOnlyManagementDependencies,\n  type PlugReadOnlyManagementTarget\n} from './plugReadOnlyManagementTarget.js';\nimport type { SavedBlePlug } from './savedBlePlug.js';\n\nexport type PlugDeviceReadOnlySettings = ShellyPlugsUiReadResult;\n\nexport const readPlugDeviceReadOnlySettingsFromTarget = async (\n  target: PlugReadOnlyManagementTarget,\n  dependencies?: PlugReadOnlyManagementDependencies\n): Promise<PlugDeviceReadOnlySettings> =>\n  withVerifiedPlugReadOnlyTransport(\n    target,\n    async (transport) => {\n      const result = await new RpcShellyPlugsUiClient(transport).readConfig();\n      return target.transport === 'bluetooth'\n        ? unwrapBlePlugReadOnlyResult(result)\n        : unwrapShellyResult(result);\n    },\n    dependencies\n  );\n\nexport const readBlePlugDeviceReadOnlySettings = async (\n  plug: Pick<SavedBlePlug, 'physicalId' | 'bleDeviceId'>\n): Promise<PlugDeviceReadOnlySettings> =>\n  readPlugDeviceReadOnlySettingsFromTarget({\n    transport: 'bluetooth',\n    physicalId: plug.physicalId,\n    bleDeviceId: plug.bleDeviceId\n  });\n''',\n'apps/mobile/src/features/plugs/flows/readSavedBlePlugDeviceSettings.ts': '''import { isRecoverableBlePlugReadOnlyError } from '../data/blePlugReadOnlyError.js';\nimport {\n  readBlePlugDeviceReadOnlySettings,\n  type PlugDeviceReadOnlySettings\n} from '../data/plugDeviceReadOnlySettings.js';\nimport type { SavedBlePlug } from '../data/savedBlePlug.js';\nimport {\n  recoverSavedBlePlugLocator,\n  type SavedBlePlugLocatorRecoveryDependencies,\n  type SavedBlePlugLocatorRecoveryOptions\n} from './recoverSavedBlePlugLocator.js';\n\nexport type SavedBlePlugDeviceSettingsRecoveryDependencies =\n  SavedBlePlugLocatorRecoveryDependencies & {\n    readSettings?(\n      plug: Pick<SavedBlePlug, 'physicalId' | 'bleDeviceId'>\n    ): Promise<PlugDeviceReadOnlySettings>;\n  };\n\nexport const readSavedBlePlugDeviceSettings = async (\n  plug: SavedBlePlug,\n  options: SavedBlePlugLocatorRecoveryOptions,\n  dependencies: SavedBlePlugDeviceSettingsRecoveryDependencies = {}\n): Promise<PlugDeviceReadOnlySettings> => {\n  const readSettings = dependencies.readSettings ?? readBlePlugDeviceReadOnlySettings;\n\n  try {\n    return await readSettings(plug);\n  } catch (error) {\n    if (!isRecoverableBlePlugReadOnlyError(error)) throw error;\n  }\n\n  const bleDeviceId = await recoverSavedBlePlugLocator(plug, options, dependencies);\n  return readSettings({ physicalId: plug.physicalId, bleDeviceId });\n};\n''',\n'apps/mobile/src/features/plugs/flows/useBlePlugDeviceSettingsFlow.ts': '''import { useQuery } from '@tanstack/react-query';\nimport type { SavedBlePlug } from '../data/savedBlePlug.js';\nimport { useSavedBlePlugStore } from '../state/savedBlePlugStore.js';\nimport { readSavedBlePlugDeviceSettings } from './readSavedBlePlugDeviceSettings.js';\n\nexport const blePlugDeviceSettingsQueryKey = (plug: SavedBlePlug | undefined) =>\n  [\n    'ble-plug-device-settings',\n    plug?.physicalId ?? 'missing',\n    plug?.bleDeviceId ?? 'missing'\n  ] as const;\n\nexport const useBlePlugDeviceSettingsFlow = (plug: SavedBlePlug | undefined) => {\n  const replaceLocator = useSavedBlePlugStore((state) => state.replaceLocator);\n\n  return useQuery({\n    queryKey: blePlugDeviceSettingsQueryKey(plug),\n    queryFn: () => {\n      if (!plug) throw new Error('Saved BLE Plug is missing.');\n      return readSavedBlePlugDeviceSettings(plug, { persistLocator: replaceLocator });\n    },\n    enabled: Boolean(plug),\n    retry: false,\n    refetchOnMount: 'always',\n    refetchOnWindowFocus: true,\n    refetchOnReconnect: true\n  });\n};\n''',\n'apps/mobile/src/features/plugs/components/BlePlugDeviceReadOnlyPanel.tsx': '''import type { ShellyPlugsUiReadResult } from '@lcl/shelly-client';\nimport { DiagnosticRow } from '@lcl/ui';\nimport { useTranslation } from '../../../app/i18n.js';\nimport { deviceButtonModeCopy } from '../../../app/locales/deviceButtonMode.js';\nimport { deviceLedCopy } from '../../../app/locales/deviceLed.js';\nimport './PlugSettingsSurface.css';\n\nexport type BlePlugDeviceReadOnlyPanelProps = {\n  settings?: ShellyPlugsUiReadResult | undefined;\n  loading?: boolean;\n  error?: boolean;\n};\n\nexport const BlePlugDeviceReadOnlyPanel = ({\n  settings,\n  loading = false,\n  error = false\n}: BlePlugDeviceReadOnlyPanelProps) => {\n  const { locale, t } = useTranslation();\n  const ledCopy = deviceLedCopy[locale];\n  const buttonCopy = deviceButtonModeCopy[locale];\n\n  if (loading) {\n    return (\n      <section className=\"plug-detail-framed-section\">\n        <h3 className=\"plug-detail-framed-section__title\">\n          {t('hardware.shelly.settings')}\n        </h3>\n        <div className=\"plug-detail-loading\" role=\"status\">\n          <span className=\"plug-detail-loading__spinner\" aria-hidden=\"true\" />\n          <span>{t('common.refreshing')}</span>\n        </div>\n      </section>\n    );\n  }\n\n  if (error) {\n    return (\n      <section className=\"plug-detail-framed-section\">\n        <h3 className=\"plug-detail-framed-section__title\">\n          {t('hardware.shelly.settings')}\n        </h3>\n        <p className=\"plug-settings-feedback plug-settings-feedback--warning\">\n          {t('dashboard.readFailed')}\n        </p>\n      </section>\n    );\n  }\n\n  if (!settings?.supported) {\n    return (\n      <section className=\"plug-detail-framed-section\">\n        <h3 className=\"plug-detail-framed-section__title\">\n          {t('hardware.shelly.settings')}\n        </h3>\n        <p className=\"plug-settings-feedback\">{ledCopy.unsupported}</p>\n      </section>\n    );\n  }\n\n  const { config, capabilities, controlCapabilities } = settings;\n  const ledModeLabel =\n    config.leds.mode === 'power'\n      ? ledCopy.power\n      : config.leds.mode === 'switch'\n        ? ledCopy.switch\n        : ledCopy.off;\n  const nightMode = config.leds.night_mode;\n  const buttonMode = config.controls?.['switch:0']?.in_mode;\n  const nightModeValue = nightMode?.enable\n    ? [\n        t('common.enabled'),\n        `${nightMode.brightness}%`,\n        nightMode.active_between.length === 2\n          ? `${nightMode.active_between[0]}–${nightMode.active_between[1]}`\n          : null\n      ]\n        .filter(Boolean)\n        .join(' · ')\n    : t('common.disabled');\n\n  return (\n    <section className=\"plug-detail-framed-section\">\n      <h3 className=\"plug-detail-framed-section__title\">\n        {t('hardware.shelly.settings')}\n      </h3>\n      <div className=\"plug-info-grid\">\n        <DiagnosticRow label={ledCopy.currentMode} value={ledModeLabel} />\n        {config.leds.mode === 'power' && capabilities.powerBrightness && (\n          <DiagnosticRow\n            label={ledCopy.powerBrightness}\n            value={`${config.leds.colors?.power?.brightness ?? 0}%`}\n          />\n        )}\n        {capabilities.nightMode && (\n          <DiagnosticRow label={ledCopy.nightMode} value={nightModeValue} />\n        )}\n        {controlCapabilities.buttonInputMode && buttonMode && (\n          <DiagnosticRow\n            label={buttonCopy.currentMode}\n            value={\n              buttonMode === 'momentary' ? buttonCopy.momentary : buttonCopy.detached\n            }\n          />\n        )}\n      </div>\n    </section>\n  );\n};\n''',\n'apps/mobile/src/features/plugs/components/BlePlugDeviceReadOnlyPanel.test.tsx': '''import { render, screen } from '@testing-library/react';\nimport { afterEach, beforeEach, describe, expect, it } from 'vitest';\nimport { I18nProvider, setLocalePreference } from '../../../app/i18n.js';\nimport { BlePlugDeviceReadOnlyPanel } from './BlePlugDeviceReadOnlyPanel.js';\n\ndescribe('BlePlugDeviceReadOnlyPanel', () => {\n  beforeEach(() => setLocalePreference('en'));\n  afterEach(() => setLocalePreference('system'));\n\n  it('renders compact read-only LED and physical-button state without mutation controls', () => {\n    render(\n      <I18nProvider>\n        <BlePlugDeviceReadOnlyPanel\n          settings={{\n            supported: true,\n            config: {\n              leds: {\n                mode: 'switch',\n                colors: {\n                  'switch:0': {\n                    on: { rgb: [0, 100, 0], brightness: 100 },\n                    off: { rgb: [100, 0, 0], brightness: 100 }\n                  }\n                },\n                night_mode: {\n                  enable: true,\n                  brightness: 10,\n                  active_between: ['22:00', '06:00']\n                }\n              },\n              controls: { 'switch:0': { in_mode: 'momentary' } }\n            },\n            capabilities: {\n              switchColors: true,\n              powerBrightness: false,\n              nightMode: true\n            },\n            controlCapabilities: { buttonInputMode: true }\n          }}\n        />\n      </I18nProvider>\n    );\n\n    expect(screen.getByText('Show ON/OFF')).toBeVisible();\n    expect(screen.getByText('enabled · 10% · 22:00–06:00')).toBeVisible();\n    expect(screen.getByText('Controls relay')).toBeVisible();\n    expect(screen.queryByRole('button')).not.toBeInTheDocument();\n  });\n});\n''',\n'apps/mobile/src/features/plugs/flows/readSavedBlePlugDeviceSettings.test.ts': '''import { describe, expect, it, vi } from 'vitest';\nimport { BlePlugReadOnlyError } from '../data/blePlugReadOnlyError.js';\nimport type { PlugDeviceReadOnlySettings } from '../data/plugDeviceReadOnlySettings.js';\nimport type {\n  PlugBleAdvertisement,\n  VerifiedPlugBleCandidate\n} from '../data/plugBleOnboarding.js';\nimport type { SavedBlePlug } from '../data/savedBlePlug.js';\nimport { readSavedBlePlugDeviceSettings } from './readSavedBlePlugDeviceSettings.js';\n\nconst plug: SavedBlePlug = {\n  physicalId: 'shellyplugsg3-aabbccddeeff',\n  name: 'Growbox fan',\n  bleDeviceId: 'stale-locator',\n  advertisementName: 'ShellyPlugSG3-AABBCCDDEEFF',\n  model: 'S3PL-00112EU',\n  generation: 3,\n  firmwareId: '1.7.5',\n  matterEnabled: false\n};\n\nconst settings: PlugDeviceReadOnlySettings = {\n  supported: true,\n  config: { leds: { mode: 'off' } },\n  capabilities: { switchColors: false, powerBrightness: false, nightMode: false },\n  controlCapabilities: { buttonInputMode: false }\n};\n\nconst advertisement: PlugBleAdvertisement = {\n  deviceId: 'fresh-locator',\n  name: plug.advertisementName,\n  rssi: -42\n};\n\nconst verified: VerifiedPlugBleCandidate = {\n  bleDeviceId: advertisement.deviceId,\n  advertisementName: advertisement.name,\n  rssi: advertisement.rssi,\n  physicalId: plug.physicalId,\n  model: plug.model,\n  generation: plug.generation,\n  firmwareId: plug.firmwareId,\n  matterEnabled: plug.matterEnabled\n};\n\nconst staleReadError = () =>\n  new BlePlugReadOnlyError({\n    kind: 'shelly-offline',\n    userMessageKey: 'errors.shellyOffline',\n    technicalMessage: 'stale GATT locator',\n    retryable: true\n  });\n\ndescribe('readSavedBlePlugDeviceSettings', () => {\n  it('uses a healthy locator without scanning', async () => {\n    const readSettings = vi.fn(async () => settings);\n    const scanCandidates = vi.fn(async () => [] as PlugBleAdvertisement[]);\n\n    await expect(\n      readSavedBlePlugDeviceSettings(\n        plug,\n        { persistLocator: vi.fn() },\n        { readSettings, scanCandidates, inspectCandidate: vi.fn() }\n      )\n    ).resolves.toBe(settings);\n\n    expect(readSettings).toHaveBeenCalledOnce();\n    expect(scanCandidates).not.toHaveBeenCalled();\n  });\n\n  it('recovers a stale locator and retries the read exactly once', async () => {\n    const readSettings = vi.fn(\n      async (target: Pick<SavedBlePlug, 'physicalId' | 'bleDeviceId'>) => {\n        if (target.bleDeviceId === plug.bleDeviceId) throw staleReadError();\n        return settings;\n      }\n    );\n    const persistLocator = vi.fn();\n\n    await expect(\n      readSavedBlePlugDeviceSettings(\n        plug,\n        { persistLocator },\n        {\n          readSettings,\n          scanCandidates: vi.fn(async () => [advertisement]),\n          inspectCandidate: vi.fn(async () => verified)\n        }\n      )\n    ).resolves.toBe(settings);\n\n    expect(readSettings).toHaveBeenCalledTimes(2);\n    expect(readSettings).toHaveBeenLastCalledWith({\n      physicalId: plug.physicalId,\n      bleDeviceId: verified.bleDeviceId\n    });\n    expect(persistLocator).toHaveBeenCalledWith(plug.physicalId, verified.bleDeviceId);\n  });\n});\n'''\n}\nfor path, content in files.items():\n    p = Path(path)\n    if p.exists():\n        raise SystemExit(f'{path} already exists')\n    p.write_text(content)\nPY",
    "pnpm exec prettier --write packages/shelly-client/src/plugsUi.ts packages/shelly-client/src/__tests__/plugsUi.test.ts apps/mobile/src/features/plugs/data/plugReadOnlyManagementTarget.ts apps/mobile/src/features/plugs/data/plugDeviceReadOnlySettings.ts apps/mobile/src/features/plugs/flows/readSavedBlePlugDeviceSettings.ts apps/mobile/src/features/plugs/flows/readSavedBlePlugDeviceSettings.test.ts apps/mobile/src/features/plugs/flows/useBlePlugDeviceSettingsFlow.ts apps/mobile/src/features/plugs/components/BlePlugDeviceReadOnlyPanel.tsx apps/mobile/src/features/plugs/components/BlePlugDeviceReadOnlyPanel.test.tsx apps/mobile/src/features/plugs/screens/BlePlugDetailScreen.tsx",
    "pnpm --filter @lcl/shelly-client test -- plugsUi.test.ts && pnpm --filter @lcl/mobile test -- src/features/plugs/components/BlePlugDeviceReadOnlyPanel.test.tsx src/features/plugs/flows/readSavedBlePlugDeviceSettings.test.ts src/features/plugs/data/plugReadOnlyManagementTarget.test.ts && pnpm --filter @lcl/mobile typecheck && pnpm --filter @lcl/shelly-client typecheck && pnpm quality:repo && git diff --check",
    "git status --short && git diff --stat && git diff -- packages/shelly-client/src/plugsUi.ts apps/mobile/src/features/plugs/data/plugReadOnlyManagementTarget.ts apps/mobile/src/features/plugs/screens/BlePlugDetailScreen.tsx"
  ]
}

{
  "id": "20260909-android-emulator-smoke-v1",
  "agent_binding": "e75c77cb-7589-4452-94b2-decc97ff85a1",
  "mode": "commands",
  "work_branch": "work/pre-phone-hardening-v1",
  "allow_write": true,
  "resources": [],
  "memory_limit_mb": 4096,
  "command_timeout": 5400,
  "idle_timeout": 1800,
  "task_timeout": 10800,
  "commands": [
    "git fetch --prune origin && test \"$(git rev-parse origin/work/pre-phone-hardening-v1)\" = \"f9ecfbb549a8eb365395030ab1bbb3b284ed54f9\"",
    "git checkout -B work/pre-phone-hardening-v1 origin/work/pre-phone-hardening-v1 && git reset --hard origin/work/pre-phone-hardening-v1 && git clean -fd",
    "set -e; export ANDROID_SDK_ROOT=\"$HOME/Library/Android/sdk\"; export ANDROID_HOME=\"$ANDROID_SDK_ROOT\"; SDKMANAGER=$(command -v sdkmanager); AVDMANAGER=$(command -v avdmanager); test -x \"$SDKMANAGER\"; test -x \"$AVDMANAGER\"; \"$SDKMANAGER\" --sdk_root=\"$ANDROID_SDK_ROOT\" 'platform-tools' 'platforms;android-36' 'build-tools;36.0.0' 'emulator' 'system-images;android-36;google_apis;arm64-v8a'; printf 'ANDROID_SDK_ROOT=%s\\n' \"$ANDROID_SDK_ROOT\"",
    "pnpm --filter @lcl/mobile build && pnpm --filter @lcl/mobile exec cap sync android",
    "set -e; export ANDROID_SDK_ROOT=\"$HOME/Library/Android/sdk\"; export ANDROID_HOME=\"$ANDROID_SDK_ROOT\"; printf 'sdk.dir=%s\\n' \"$ANDROID_SDK_ROOT\" > apps/mobile/android/local.properties; cd apps/mobile/android; ./gradlew --no-daemon assembleDebug lintDebug",
    "set -e; export ANDROID_SDK_ROOT=\"$HOME/Library/Android/sdk\"; export ANDROID_HOME=\"$ANDROID_SDK_ROOT\"; export PATH=\"$ANDROID_SDK_ROOT/platform-tools:$ANDROID_SDK_ROOT/emulator:$PATH\"; AVD=lcl_api36_smoke; if ! avdmanager list avd | grep -q \"Name: $AVD\"; then echo no | avdmanager create avd -f -n \"$AVD\" -k 'system-images;android-36;google_apis;arm64-v8a' --device pixel_6; fi; adb kill-server || true; adb start-server; nohup emulator -avd \"$AVD\" -no-window -no-audio -no-boot-anim -no-snapshot -wipe-data -gpu swiftshader_indirect >/tmp/lcl-emulator.log 2>&1 & echo $! >/tmp/lcl-emulator.pid; adb wait-for-device; for i in $(seq 1 120); do BOOT=$(adb shell getprop sys.boot_completed 2>/dev/null | tr -d '\\r'); [ \"$BOOT\" = \"1\" ] && break; sleep 2; done; test \"$(adb shell getprop sys.boot_completed | tr -d '\\r')\" = \"1\"; adb shell input keyevent 82 || true; adb shell settings put global window_animation_scale 0; adb shell settings put global transition_animation_scale 0; adb shell settings put global animator_duration_scale 0; adb shell getprop ro.build.version.release; adb shell getprop ro.build.version.sdk",
    "set -e; export ANDROID_SDK_ROOT=\"$HOME/Library/Android/sdk\"; export PATH=\"$ANDROID_SDK_ROOT/platform-tools:$PATH\"; APK=apps/mobile/android/app/build/outputs/apk/debug/app-debug.apk; test -f \"$APK\"; adb install -r \"$APK\"; adb shell dumpsys package link.localclimate.app | grep -E 'versionName=|versionCode=|targetSdk=|minSdk=' | head -20; adb shell pm list permissions -g >/dev/null; adb logcat -c; adb shell am force-stop link.localclimate.app; adb shell monkey -p link.localclimate.app -c android.intent.category.LAUNCHER 1 >/tmp/lcl-monkey-1.log; sleep 5; PID=$(adb shell pidof link.localclimate.app | tr -d '\\r'); test -n \"$PID\"; printf 'APP_PID=%s\\n' \"$PID\"; adb shell dumpsys activity activities | grep -m1 'mResumedActivity' | grep 'link.localclimate.app'; adb logcat --pid=\"$PID\" -d '*:W' > /tmp/lcl-app-logcat-1.txt || true; ! grep -E 'FATAL EXCEPTION|AndroidRuntime.*FATAL|Process link.localclimate.app.*has died' /tmp/lcl-app-logcat-1.txt",
    "set -e; export ANDROID_SDK_ROOT=\"$HOME/Library/Android/sdk\"; export PATH=\"$ANDROID_SDK_ROOT/platform-tools:$PATH\"; adb shell am force-stop link.localclimate.app; sleep 1; adb shell monkey -p link.localclimate.app -c android.intent.category.LAUNCHER 1 >/tmp/lcl-monkey-2.log; sleep 4; PID=$(adb shell pidof link.localclimate.app | tr -d '\\r'); test -n \"$PID\"; adb shell am broadcast -a android.intent.action.CONFIGURATION_CHANGED >/dev/null || true; adb shell settings put system user_rotation 1 || true; sleep 2; adb shell settings put system user_rotation 0 || true; sleep 2; adb shell dumpsys activity activities | grep -m1 'mResumedActivity' | grep 'link.localclimate.app'; adb logcat --pid=\"$PID\" -d '*:W' > /tmp/lcl-app-logcat-2.txt || true; ! grep -E 'FATAL EXCEPTION|AndroidRuntime.*FATAL|Process link.localclimate.app.*has died' /tmp/lcl-app-logcat-2.txt; printf '%s\\n' 'ANDROID_EMULATOR_SMOKE=PASS'",
    "set -e; export ANDROID_SDK_ROOT=\"$HOME/Library/Android/sdk\"; export PATH=\"$ANDROID_SDK_ROOT/platform-tools:$ANDROID_SDK_ROOT/emulator:$PATH\"; adb emu kill || true; sleep 2; test -z \"$(git diff --name-only -- packages/automation-core packages/script-generator)\"; git reset --hard origin/work/pre-phone-hardening-v1; git clean -fd; printf 'COMMIT_SHA=%s\\n' \"$(git rev-parse HEAD)\"; test -z \"$(git status --porcelain)\""
  ]
}

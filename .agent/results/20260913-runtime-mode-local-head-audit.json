{
  "id": "20260913-runtime-mode-local-head-audit",
  "status": "done",
  "mode": "commands",
  "work_branch": "work/device-rule-decoupling-20260913",
  "allow_write": false,
  "started_at": "2026-09-13T18:04:07.839764+00:00",
  "command_timeout": 600,
  "edits": {},
  "commands": [
    {
      "command": "git fetch origin agent-control && git show origin/agent-control:.agent/scripts/20260913-runtime-mode-local-head-audit.sh > /tmp/runtime-mode-local-head-audit.sh && /bin/bash /tmp/runtime-mode-local-head-audit.sh",
      "exit_code": 0,
      "output": "From https://github.com/MichalMatu/local-climate-link-starter\n * branch              agent-control -> FETCH_HEAD\n   061ddc2b..3741a88c  agent-control -> origin/agent-control\nLOCAL_HEAD=83f889a424ed1128c34b8586118f0c58e6ae9d26\nLOCAL_BRANCH=agent-work\nORIGIN_HEAD=83f889a424ed1128c34b8586118f0c58e6ae9d26\nSTATUS_BEGIN\nSTATUS_END\nLOG_BEGIN\n83f889a4 (HEAD -> agent-work, origin/work/device-rule-decoupling-20260913) Wire standalone device navigation\n71b6c8f3 Fix mobile navigation refactor checks\n3aec30c7 Record device management hardware validation\nLOG_END\nDIFF_STAT_BEGIN\nDIFF_STAT_END\nDETAIL_TEST_BEGIN\n};\n\nconst renderDetail = (installationId: string, onBack = vi.fn()) => {\n  const queryClient = new QueryClient({\n    defaultOptions: { queries: { retry: false }, mutations: { retry: false } }\n  });\n  return {\n    onBack,\n    ...render(\n      <I18nProvider>\n        <QueryClientProvider client={queryClient}>\n          <InstallationDetailScreen installationId={installationId} onBack={onBack} />\n        </QueryClientProvider>\n      </I18nProvider>\n    )\n  };\n};\n\ndescribe('InstallationDetailScreen', () => {\n  beforeEach(() => {\n    setLocalePreference('pl');\n    resetInstalledAutomationStore();\n    vi.restoreAllMocks();\n  });\n\n  afterEach(() => {\n    cleanup();\n    resetInstalledAutomationStore();\n    vi.unstubAllGlobals();\n  });\n\n  it('shows a stable not-found route instead of falling back to another installation', () => {\n    renderDetail('missing-installation');\n    expect(\n      screen.getByRole('heading', { name: 'Nie znaleziono automatyki' })\n    ).toBeVisible();\n  });\n\n  it('shows Shelly runtime and safely pauses and resumes the exact installed script', async () => {\n    const saved = installation();\n    useInstalledAutomationStore.getState().upsertInstallation(saved);\n    const { rpcMethods } = installShellyFetchMock();\n    const { onBack } = renderDetail(saved.id);\n\n    expect(await screen.findByText('21.4°C')).toBeVisible();\n    expect(screen.getByRole('heading', { name: 'Salon' })).toBeVisible();\n    expect(screen.getByText('Sterowanie temperaturą')).toBeVisible();\n    expect(screen.getByText('55.2%')).toBeVisible();\n    expect(screen.getByText('1.31 kPa')).toBeVisible();\n    expect(screen.getAllByText('19°C / 20°C').length).toBeGreaterThanOrEqual(1);\n    expect(screen.getByText('Przedpokój')).toBeVisible();\n    expect(\n      document.querySelector('.installation-detail-header .detail-back-link')\n    ).toBeNull();\n    expect(\n      document.querySelector('.installation-detail-header .runtime-refresh-action')\n    ).toBeNull();\n    expect(\n      document.querySelectorAll('.installation-detail-shell svg:not(.tabler-icon)')\n    ).toHaveLength(0);\n\n    const automationCard = screen\n      .getByRole('heading', { name: 'Automatyka' })\n      .closest('article');\n    expect(automationCard).not.toBeNull();\n    const auto = within(automationCard!).getByRole('button', { name: 'AUTO' });\n    const manual = within(automationCard!).getByRole('button', { name: 'MANUAL' });\n    const relayOnButton = within(automationCard!).getByRole('button', { name: 'ON' });\n    const relayOffButton = within(automationCard!).getByRole('button', { name: 'OFF' });\n    await waitFor(() => expect(auto).toHaveAttribute('aria-pressed', 'true'));\n    expect(relayOnButton).toBeDisabled();\n    expect(relayOffButton).toBeDisabled();\n\n    fireEvent.click(manual);\n\n    const toastRegion = await screen.findByRole('region', { name: 'Powiadomienia' });\n    expect(\n      await within(toastRegion).findByText(\n        'Automatyka zatrzymana, wyjście potwierdzone jako OFF.'\n      )\n    ).toBeVisible();\n    await waitFor(() => expect(manual).toHaveAttribute('aria-pressed', 'true'));\n    await waitFor(() => expect(relayOnButton).toBeEnabled());\n    expect(relayOffButton).toBeEnabled();\n    expect(relayOffButton).toHaveAttribute('aria-pressed', 'true');\n    fireEvent.click(relayOnButton);\n    await waitFor(() => expect(relayOnButton).toHaveAttribute('aria-pressed', 'true'));\n    fireEvent.click(relayOffButton);\n    await waitFor(() => expect(relayOffButton).toHaveAttribute('aria-pressed', 'true'));\n    expect(screen.queryByRole('heading', { name: 'Skrypt zatrzymany' })).toBeNull();\n    expect(rpcMethods).toContain('Script.Eval');\n    expect(rpcMethods).not.toContain('Script.Stop');\n    expect(screen.getByText('21.4°C')).toBeVisible();\n    expect(rpcMethods).toContain('Switch.Set');\n\n    fireEvent.click(auto);\n\n    expect(await within(toastRegion).findByText('Automatyka uruchomiona.')).toBeVisible();\n    await waitFor(() => expect(auto).toHaveAttribute('aria-pressed', 'true'));\n    expect(rpcMethods).not.toContain('Script.Start');\n    expect(screen.getByText('21.4°C')).toBeVisible();\n\n    expect(onBack).not.toHaveBeenCalled();\n  });\n\n  it('opens scoped technical diagnostics without duplicating the installation summary', async () => {\n    const saved = installation();\n    useInstalledAutomationStore.getState().upsertInstallation(saved);\n    const { rpcMethods } = installShellyFetchMock();\n    renderDetail(saved.id);\n    expect(await screen.findByText('21.4°C')).toBeVisible();\n    fireEvent.click(screen.getByRole('button', { name: 'Diagnostyka' }));\n    const dialog = await screen.findByRole('dialog', { name: 'Diagnostyka · Salon' });\n    expect(await within(dialog).findByText('JS użyte teraz')).toBeVisible();\n    expect(within(dialog).getByText('CPU skryptu')).toBeVisible();\n    expect(within(dialog).getByText('RAM Shelly wolny')).toBeInTheDocument();\n    expect(within(dialog).getByText('Bateria')).toBeInTheDocument();\n    expect(within(dialog).getByText('RSSI')).toBeInTheDocument();\n    expect(within(dialog).queryByText('Temperatura', { exact: true })).toBeNull();\n    expect(within(dialog).queryByText('Wilgotność', { exact: true })).toBeNull();\n    expect(within(dialog).queryByText('VPD', { exact: true })).toBeNull();\n    expect(\n      within(dialog).queryByText('Aktywne progi ON / OFF', { exact: true })\n    ).toBeNull();\n    expect(within(dialog).queryByText('Wyjście', { exact: true })).toBeNull();\n    await waitFor(() => expect(rpcMethods).toContain('Script.GetStatus'));\nDETAIL_TEST_END\nDETAIL_MOCK_REFERENCES_BEGIN\n146-          wifi: { rssi: -55 },\n147-          sys: {\n148-            time: '14:00',\n149-            unixtime: 1_782_820_000,\n150-            uptime: 3600,\n151-            last_sync_ts: 1_782_819_900\n152-          }\n153-        };\n154-        break;\n155-      case 'Script.List': {\n156-        const scriptId = options.scriptId === undefined ? 1 : options.scriptId;\n157-        result = {\n158-          scripts:\n159-            scriptId === null\n160-              ? []\n161-              : [\n162-                  {\n163-                    id: scriptId,\n164-                    name: 'Local Climate Link Thermostat',\n165-                    enable: true,\n166:                    running: scriptRunning\n167-                  }\n168-                ]\n169-        };\n170-        break;\n171-      }\n172-      case 'Script.GetCode':\n173-        result = { data: '// deployed exact source', left: 0 };\n174-        break;\n175-      case 'Script.GetStatus':\n176-        result = {\n177:          running: scriptRunning,\n178-          mem_used: 2660,\n179-          mem_peak: 6804,\n180-          mem_free: 22442,\n181-          cpu: 15.2\n182-        };\n183-        break;\n184-      case 'Sys.GetStatus':\n185-        result = { ram_size: 259128, ram_free: 90000 };\n186-        break;\n187:      case 'Script.Eval': {\n188-        const code = body.params?.code ?? '';\n189-        if (code.includes('R.m=1')) {\n190-          runtimeMode = 1;\n191-        } else if (code.includes('R.m=0')) {\n192-          runtimeMode = 0;\n193-        }\n194-        result = { result: String(runtimeMode) };\n195-        break;\n196-      }\n197:      case 'Script.Stop':\n198-        scriptRunning = false;\n199-        result = null;\n200-        break;\n201:      case 'Script.Start':\n202-        scriptRunning = true;\n203-        result = null;\n204-        break;\n205-      case 'Switch.Set':\n206-        relayOn = body.params?.on ?? false;\n207-        result = null;\n208-        break;\n209-      default:\n210-        result = {};\n211-    }\n212-\n213-    return jsonResponse({ id: body.id ?? 1, result });\n214-  });\n215-\n216-  vi.stubGlobal('fetch', fetchMock);\n217-  return { rpcMethods };\n218-};\n219-\n220-const timeInstallation = () =>\n221-  createTimeInstalledAutomation({\n222-    shelly: { id: 'shellyplugsg3-time-detail', model: 'S3PL-00112EU', gen: 3 },\n223-    shellyName: 'Lampa',\n224-    baseUrl: 'http://192.168.0.21/',\n225-    onJobId: 7,\n226-    offJobId: 8,\n227-    config: { relayId: 0, onTime: '08:00', offTime: '20:00' },\n228-    nowMs: 1000\n229-  });\n230-\n231-const installTimeShellyFetchMock = () => {\n232-  let relayOn = true;\n233-  let rev = 1;\n234-  let jobs: ShellyScheduleJob[] = [\n235-    {\n236-      id: 7,\n237-      enable: true,\n238-      timespec: dailyScheduleTimespec('08:00'),\n239-      calls: [{ method: 'Switch.Set', params: { id: 0, on: true } }]\n240-    },\n241-    {\n242-      id: 8,\n243-      enable: true,\n244-      timespec: dailyScheduleTimespec('20:00'),\n245-      calls: [{ method: 'Switch.Set', params: { id: 0, on: false } }]\n246-    }\n247-  ];\n248-  const rpcMethods: string[] = [];\n249-\n250-  const fetchMock = vi.fn(async (_input: RequestInfo | URL, init?: RequestInit) => {\n251-    const body = JSON.parse(String(init?.body ?? '{}')) as {\n252-      id?: number | string;\n253-      method?: string;\n254-      params?: {\n255-        id?: number;\n256-        on?: boolean;\n--\n400-    expect(relayOnButton).toBeDisabled();\n401-    expect(relayOffButton).toBeDisabled();\n402-\n403-    fireEvent.click(manual);\n404-\n405-    const toastRegion = await screen.findByRole('region', { name: 'Powiadomienia' });\n406-    expect(\n407-      await within(toastRegion).findByText(\n408-        'Automatyka zatrzymana, wyjście potwierdzone jako OFF.'\n409-      )\n410-    ).toBeVisible();\n411-    await waitFor(() => expect(manual).toHaveAttribute('aria-pressed', 'true'));\n412-    await waitFor(() => expect(relayOnButton).toBeEnabled());\n413-    expect(relayOffButton).toBeEnabled();\n414-    expect(relayOffButton).toHaveAttribute('aria-pressed', 'true');\n415-    fireEvent.click(relayOnButton);\n416-    await waitFor(() => expect(relayOnButton).toHaveAttribute('aria-pressed', 'true'));\n417-    fireEvent.click(relayOffButton);\n418-    await waitFor(() => expect(relayOffButton).toHaveAttribute('aria-pressed', 'true'));\n419-    expect(screen.queryByRole('heading', { name: 'Skrypt zatrzymany' })).toBeNull();\n420:    expect(rpcMethods).toContain('Script.Eval');\n421:    expect(rpcMethods).not.toContain('Script.Stop');\n422-    expect(screen.getByText('21.4°C')).toBeVisible();\n423-    expect(rpcMethods).toContain('Switch.Set');\n424-\n425-    fireEvent.click(auto);\n426-\n427-    expect(await within(toastRegion).findByText('Automatyka uruchomiona.')).toBeVisible();\n428-    await waitFor(() => expect(auto).toHaveAttribute('aria-pressed', 'true'));\n429:    expect(rpcMethods).not.toContain('Script.Start');\n430-    expect(screen.getByText('21.4°C')).toBeVisible();\n431-\n432-    expect(onBack).not.toHaveBeenCalled();\n433-  });\n434-\n435-  it('opens scoped technical diagnostics without duplicating the installation summary', async () => {\n436-    const saved = installation();\n437-    useInstalledAutomationStore.getState().upsertInstallation(saved);\n438-    const { rpcMethods } = installShellyFetchMock();\n439-    renderDetail(saved.id);\n440-    expect(await screen.findByText('21.4°C')).toBeVisible();\n441-    fireEvent.click(screen.getByRole('button', { name: 'Diagnostyka' }));\n442-    const dialog = await screen.findByRole('dialog', { name: 'Diagnostyka · Salon' });\n443-    expect(await within(dialog).findByText('JS użyte teraz')).toBeVisible();\n444-    expect(within(dialog).getByText('CPU skryptu')).toBeVisible();\n445-    expect(within(dialog).getByText('RAM Shelly wolny')).toBeInTheDocument();\n446-    expect(within(dialog).getByText('Bateria')).toBeInTheDocument();\n447-    expect(within(dialog).getByText('RSSI')).toBeInTheDocument();\n448-    expect(within(dialog).queryByText('Temperatura', { exact: true })).toBeNull();\n449-    expect(within(dialog).queryByText('Wilgotność', { exact: true })).toBeNull();\n450-    expect(within(dialog).queryByText('VPD', { exact: true })).toBeNull();\n451-    expect(\n452-      within(dialog).queryByText('Aktywne progi ON / OFF', { exact: true })\n453-    ).toBeNull();\n454-    expect(within(dialog).queryByText('Wyjście', { exact: true })).toBeNull();\n455-    await waitFor(() => expect(rpcMethods).toContain('Script.GetStatus'));\n456-    expect(rpcMethods).toContain('Sys.GetStatus');\n457-    const before = rpcMethods.filter((method) => method === 'Script.GetStatus').length;\n458-    fireEvent.click(within(dialog).getByRole('button', { name: 'Odśwież' }));\n459-    await waitFor(() =>\n460-      expect(\n461-        rpcMethods.filter((method) => method === 'Script.GetStatus').length\n462-      ).toBeGreaterThan(before)\n463-    );\n464-    fireEvent.click(within(dialog).getByRole('button', { name: 'Zamknij' }));\n465-    expect(screen.queryByRole('dialog', { name: 'Diagnostyka · Salon' })).toBeNull();\n466-  });\n467-\n468-  it('shows the current deployed script for the saved climate automation', async () => {\n469-    const saved = installation();\n470-    useInstalledAutomationStore.getState().upsertInstallation(saved);\n471-    const { rpcMethods } = installShellyFetchMock();\n472-\n473-    renderDetail(saved.id);\n474-\n475-    const showScript = await screen.findByRole('button', {\n476-      name: 'Pokaż wdrożony skrypt'\n477-    });\n478-    await waitFor(() => expect(showScript).toBeEnabled());\n479-\n480-    fireEvent.click(showScript);\n481-\n482-    const dialog = await screen.findByRole('dialog', {\n483-      name: 'Skrypt wdrożony w Shelly'\n484-    });\n--\n503-      heading: 'Problem właściciela wyjścia'\n504-    }\n505-  ])('offers read-only recovery for $name state', async ({ options, heading }) => {\n506-    const saved = installation();\n507-    useInstalledAutomationStore.getState().upsertInstallation(saved);\n508-    const { rpcMethods } = installShellyFetchMock(options);\n509-\n510-    renderDetail(saved.id);\n511-\n512-    expect(await screen.findByRole('heading', { name: heading })).toBeVisible();\n513-    if (options.scriptId === 2) {\n514-      expect(\n515-        screen.getByRole('button', { name: 'Pokaż wdrożony skrypt' })\n516-      ).toBeDisabled();\n517-    }\n518-    const refresh = screen.getByRole('button', { name: 'Sprawdź ponownie' });\n519-    expect(refresh).toBeVisible();\n520-\n521-    fireEvent.click(refresh);\n522-    expect(await screen.findByRole('heading', { name: heading })).toBeVisible();\n523:    expect(rpcMethods).not.toContain('Script.Start');\n524:    expect(rpcMethods).not.toContain('Script.Stop');\n525-    expect(rpcMethods).not.toContain('Switch.Set');\n526-  });\n527-\n528-  it('manages a native daily schedule end to end without a climate script owner', async () => {\n529-    const saved = timeInstallation();\n530-    useInstalledAutomationStore.getState().upsertInstallation(saved);\n531-    const shelly = installTimeShellyFetchMock();\n532-    const onBack = vi.fn();\n533-\n534-    renderDetail(saved.id, onBack);\n535-\n536-    expect(await screen.findByText('Działa')).toBeVisible();\n537-    expect(screen.getByRole('heading', { name: 'Lampa' })).toBeVisible();\n538-    expect(screen.getAllByText('08:00').length).toBeGreaterThanOrEqual(1);\n539-    expect(screen.getAllByText('20:00').length).toBeGreaterThanOrEqual(1);\n540-    expect(screen.getByText('Natywny Shelly Schedule')).toBeVisible();\n541-    expect(screen.getByText('ON', { exact: true })).toBeVisible();\n542-\n543-    fireEvent.click(screen.getByRole('button', { name: 'Wstrzymaj automatykę' }));\n544-    expect(await screen.findByText('Wstrzymana')).toBeVisible();\n545-    expect(shelly.relayOn).toBe(false);\n546-    expect(shelly.jobs.every((job) => !job.enable)).toBe(true);\n547-\n548-    fireEvent.click(screen.getByRole('button', { name: 'Wznów automatykę' }));\n549-    expect(await screen.findByText('Działa')).toBeVisible();\n550-    expect(shelly.relayOn).toBe(true);\n551-    expect(shelly.jobs.every((job) => job.enable)).toBe(true);\n552-\n553-    fireEvent.change(screen.getByLabelText('Włącz o'), { target: { value: '18:00' } });\n554-    fireEvent.change(screen.getByLabelText('Wyłącz o'), { target: { value: '23:00' } });\n555-    fireEvent.click(screen.getByRole('button', { name: 'Zapisz zmiany' }));\n556-\n557-    const toastRegion = await screen.findByRole('region', { name: 'Powiadomienia' });\n558-    expect(\n559-      await within(toastRegion).findByText('Harmonogram zaktualizowany.')\n560-    ).toBeVisible();\n561-    expect(shelly.jobs.find((job) => job.id === 7)?.timespec).toBe(\n562-      dailyScheduleTimespec('18:00')\n563-    );\n564-    expect(shelly.jobs.find((job) => job.id === 8)?.timespec).toBe(\n565-      dailyScheduleTimespec('23:00')\n566-    );\n567-    expect(shelly.relayOn).toBe(false);\n568-    const stored = useInstalledAutomationStore\n569-      .getState()\n570-      .installations.find((item) => item.id === saved.id);\n571-    expect(stored).toMatchObject({\n572-      kind: 'time',\n573-      config: { onTime: '18:00', offTime: '23:00' }\n574-    });\n575-\n576-    fireEvent.click(screen.getByRole('button', { name: 'Usuń automatykę czasową' }));\n577-    const dialog = screen.getByRole('dialog', { name: 'Usunąć automatykę czasową?' });\n578-    fireEvent.click(within(dialog).getByRole('button', { name: 'Potwierdź usuń' }));\n579-\nDETAIL_MOCK_REFERENCES_END\n",
      "elapsed_seconds": 1.27,
      "timed_out": false,
      "idle_timed_out": false,
      "memory_limit_mb": 2048,
      "memory_limited": false,
      "background_process_leak": false,
      "peak_rss_mb": 1.88,
      "output_policy": "stream",
      "captured_output_chars": 17617,
      "output_truncated": false,
      "current_rss_mb": 1.88,
      "stage_name": "command-1",
      "stage_index": 1,
      "stage_total": 1,
      "stage_phase": "commands"
    }
  ],
  "verification": [],
  "stages": [
    {
      "stage_name": "command-1",
      "stage_index": 1,
      "stage_total": 1,
      "stage_phase": "commands",
      "outcome": "passed",
      "elapsed_seconds": 1.27
    }
  ],
  "git_status": {
    "exit_code": 0,
    "output": "",
    "elapsed_seconds": 0.111
  },
  "git_diff": {
    "exit_code": 0,
    "output": "",
    "elapsed_seconds": 0.088
  },
  "finished_at": "2026-09-13T18:04:10.824565+00:00",
  "task_digest": "408976259fe7c23543f6b52e5942825c67eb1d60c246b61a4e5e4ac7603ddbdf",
  "idle_timeout": 180,
  "task_timeout": 1200,
  "memory_limit_mb": 2048,
  "attempt_id": "ee726b78fd722c1cba060c06",
  "daemon_version": "4.18.20"
}
